#Andrey Markov

This is a ruby irc bot to do markov chains. It's *super* ghetto.

Basically set this project up with `bundle install` then edit the configuration
in `bin/andreymarkov` to make it not conflict with the deployed one. Use
`#bag-bots` for testing.
